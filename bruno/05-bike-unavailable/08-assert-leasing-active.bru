meta {
  name: Assert the leasing is active
  type: http
  seq: 8
}

get {
  url: {{engineRest}}/history/activity-instance?processInstanceId={{processInstanceId}}&activityId=endEvent_leasingActive
  body: none
  auth: none
}

script:pre-request {
  const pid = bru.getVar("processInstanceId");
  await pollEngine(`/history/activity-instance?processInstanceId=${pid}&activityId=endEvent_leasingActive`,
    (b, s) => s === 200 && Array.isArray(b) && b.length >= 1);
}

assert {
  res.status: eq 200
  res.body: length 1
}
